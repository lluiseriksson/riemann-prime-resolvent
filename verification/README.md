# Verification records

`LEDGER.md` records durable release checkpoints. Ephemeral CI logs should be attached to GitHub Actions artifacts, not committed. A theorem moves to reproduced Lean status only when the exact tagged build and axiom oracle are green.
