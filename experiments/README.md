# Experiments

This directory is reserved for certified finite-dimensional experiments.
Exploratory floating-point notebooks should live on a separate branch and must
not be treated as proof artifacts.

The accepted path is:

```text
external interval computation -> JSON certificate -> deterministic checker -> Lean theorem
```
