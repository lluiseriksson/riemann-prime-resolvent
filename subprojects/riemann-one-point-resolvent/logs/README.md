# Verification logs

`verification.log` is produced by `scripts/verify.sh`. GitHub Actions logs remain the authoritative record for Lean compilation when the local construction environment has no Lean toolchain.
